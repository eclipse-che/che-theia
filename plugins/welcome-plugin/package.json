{
  "name": "@eclipse-che/welcome-plugin",
  "version": "0.0.1",
  "publisher": "Eclipse Che",
  "keywords": [
    "theia-plugin"
  ],
  "description": "Welcome Page Plugin",
  "license": "EPL-2.0",
  "files": [
    "src",
    "resources"
  ],
  "dependencies": {
    "@fortawesome/fontawesome-free": "5.6.3"
  },
  "devDependencies": {
    "@theia/plugin": "0.3.19",
    "@theia/plugin-packager": "latest",
    "@eclipse-che/plugin": "latest",
    "@eclipse-che/scripts": "^0.0.1"
  },
  "scripts": {
    "prepare": "yarn run clean && yarn run build",
    "clean": "che:script clean",
    "format-code": "che:script format",
    "compile": "che:script compile",
    "watch": "che:script watch",
    "tslint-fix": "che:script lint:fix",
    "tslint": "che:script lint",
    "build": "che:script lint:fix && che:script build && theia:plugin pack"
  },
  "engines": {
    "theiaPlugin": "next"
  },
  "theiaPlugin": {
    "backend": "lib/welcome-plugin.js"
  }
}
