{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.8.1"
  },
  "scripts": {
    "prepare": "yarn run bootstrap && lerna run prepare",
    "bootstrap": "lerna bootstrap --ignore-scripts",
    "publish:next": "lerna run publish:next"
  }
}
