{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.5.0"
  },
  "scripts": {
    "prepare": "lerna run prepare",
    "publish:next": "lerna publish --registry=https://registry.npmjs.org/ --exact --canary=next --npm-tag=next --force-publish --no-git-tag-version --no-push --yes"
  },
  "workspaces": [
    "extensions/*",
    "plugins/*"
  ]
}
